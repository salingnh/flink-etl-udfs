# Third-party Python dependencies imported by flink-etl-udfs worker-side code
# and therefore candidates for Flink --pyRequirements distribution.
#
# pycountry 24.6.1 is intentionally pinned because it supports Python >=3.8,
# including this project's Python 3.9 baseline. Newer pycountry 26.x requires
# Python >=3.10.
pycountry==24.6.1

# PyFlink itself is supplied by the matching Flink runtime / Python environment
# and must not be reinstalled per job through this file.
