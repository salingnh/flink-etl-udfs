# Third-party Python dependencies imported by flink-etl-udfs worker-side code
# and therefore candidates for Flink --pyRequirements distribution.
#
# Current state (v0.6.0): no third-party runtime libraries are required.
# Deterministic scalar transforms use the Python standard library. The asynchronous
# profile-enrichment client uses asyncio + urllib from the standard library.
# PyFlink wrappers import `pyflink`, which is supplied by the matching Flink runtime /
# Python environment rather than installed per job via --pyRequirements.
#
# Add future worker-side dependencies here as soon as source code imports them,
# for example:
# phonenumbers==<pinned-version>
# pydicom==<pinned-version>
#
# Keep this file production-only: do not add pytest, ruff, mypy, or build tools.
