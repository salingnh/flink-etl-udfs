# Runtime dependencies for flink-etl-udfs on PyFlink workers.
#
# IMPORTANT: Keep this version aligned with the Apache Flink cluster version.
# A mismatched PyFlink package can cause serialization, API, or runtime failures.
apache-flink==2.3.0
